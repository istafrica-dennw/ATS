package com.ats.model;

public enum InterviewStatus {
    ASSIGNED,    // Interview assigned to interviewer but not started
    IN_PROGRESS, // Interview started, responses being filled
    COMPLETED    // All responses completed and submitted
} 