package com.ats.controller.jobs;

import org.springframework.web.bind.annotation.RequestMapping;

@RequestMapping("/api/jobs")
public abstract class BaseJobController {
    // Base class for all job-related controllers
}
