# Development Dockerfile
FROM node:18-alpine

WORKDIR /app

# Copy package files
COPY package*.json ./

# Install all dependencies (including dev dependencies)
RUN npm install

# Copy the rest of the application
COPY . .

# Expose port 3002 (career portal runs on different port than admin frontend)
EXPOSE 3002

# Start development server
CMD ["npm", "start"]
